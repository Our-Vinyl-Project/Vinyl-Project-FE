import React from 'react';
import PropTypes from 'prop-types';

// I think we don't need to separate salesList and saleItem, so this file may be able to be deleted. Thoughts?

function SaleItem(props) {
  return (
    <div>
      {/* Album art
      Artist
      Album title
      condition
      price */}

    </div>
  );
}
SaleItem.propTypes = {
  
};
export default SaleItem;
